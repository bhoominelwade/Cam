# dependencies
node_modules/

# Expo / native projects are generated by `expo prebuild` (ADR-002, CNG)
app/.expo/
app/ios/
app/android/
app/dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# builds & coverage
coverage/
dist/
build/

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# env — no secrets in the repo, ever (PLAN §6.1)
.env
.env.*
